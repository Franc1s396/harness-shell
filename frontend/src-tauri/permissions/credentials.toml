[[permission]]
identifier = "credentials"
description = "Allows the main window to store or delete opaque SSH credential references."
commands.allow = [
  "store_ssh_password",
  "store_private_key_passphrase",
  "import_private_key",
  "delete_ssh_credential",
]
