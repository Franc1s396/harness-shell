{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Backend bootstrap access for the main window.",
  "windows": ["main"],
  "permissions": [
    "bootstrap",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
